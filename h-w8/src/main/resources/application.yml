spring:
  datasource:
    url: jdbc:h2:mem:testdb
    #initialization-mode: always
  #  main:
  #    allow-circular-references: true

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

logging:
  level:
    ROOT: info
